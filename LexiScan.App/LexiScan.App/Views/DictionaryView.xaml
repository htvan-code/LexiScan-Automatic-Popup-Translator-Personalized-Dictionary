<UserControl x:Class="LexiScan.App.Views.DictionaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000" Background="White">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="119*"/>
            <ColumnDefinition Width="770*"/>
            <ColumnDefinition Width="111*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="15,15,15,15" Grid.ColumnSpan="3">
            <TextBox Name="SearchTextBox" Width="350" Height="35" FontSize="16"
                     Text="Tìm kiếm trong Từ điển cá nhân..." 
                     VerticalContentAlignment="Center" Margin="0,0,10,0"/>
            <Button Content="Tra Cứu / Lọc" Name="SearchButton" Width="100"/>
        </StackPanel>

        <Grid Grid.Row="1" Grid.ColumnSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="5*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="15,0,0,15">
                <ComboBox SelectedIndex="0" Margin="0,0,0,10" HorizontalAlignment="Left" Width="200">
                    <ComboBoxItem Content="Tất cả Lịch sử"/>
                    <ComboBoxItem Content="Chỉ các Câu đã Ghim (Pinned)"/>
                    <ComboBoxItem Content="Theo Ngày Tra Cứu"/>
                </ComboBox>

                <ListBox Name="WordListBox" MinHeight="400" BorderThickness="1" BorderBrush="#CCC">
                    <ListBoxItem Content="Abstraction (Pinned)"/>
                    <ListBoxItem Content="Polymorphism"/>
                    <ListBoxItem Content="LexiScan"/>
                </ListBox>
            </StackPanel>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Stretch"
                          ResizeDirection="Columns" Width="6" Background="#E0E0E0"/>

            <ScrollViewer Grid.Column="2" Margin="10,0,15,15" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="WORD NAME (Đang được chọn)" FontSize="36" FontWeight="ExtraBold" Margin="0,0,0,10" Foreground="#FF0C39E8"/>
                    <TextBlock Text="Định nghĩa chính và Phiên âm." FontSize="16" FontStyle="Italic" Foreground="#333" Margin="0,0,0,25"/>

                    <TextBlock Text="CÁC CÂU ĐÃ GHIM (PINNED SENTENCES):" FontWeight="Bold" FontSize="16" Margin="0,10,0,5"/>
                    <StackPanel Name="PinnedSentencesPlaceholder" Margin="0,5,0,0">
                        <Border BorderBrush="#DDD" BorderThickness="1" Padding="10" Margin="0,5">
                            <TextBlock Text="&quot;This is a placeholder sentence that was pinned from the selected text.&quot; - Source: Web Browser" TextWrapping="Wrap"/>
                        </Border>
                        <Border BorderBrush="#DDD" BorderThickness="1" Padding="10" Margin="0,5">
                            <TextBlock Text="&quot;Another example of a pinned sentence.&quot; - Source: PDF Reader" TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
    