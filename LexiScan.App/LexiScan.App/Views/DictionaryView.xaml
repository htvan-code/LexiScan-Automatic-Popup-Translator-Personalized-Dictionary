<UserControl x:Class="LexiScan.App.Views.DictionaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LexiScan.App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="373.437" d:DesignWidth="740.625" 
             Background="#121212">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBox}" x:Key="SearchTextBoxStyle">
            <Setter Property="Foreground" Value="#F0F0F0"/>
        </Style>
        <Style x:Key="DropdownActionButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#E91E63"/>
        </Style>
        <Style x:Key="ListBoxItemStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="Border" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true"
                                Background="{TemplateBinding Background}">
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#442233"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#442233"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="LexiScan App" FontSize="28" FontWeight="ExtraBold" Margin="0,0,0,10" Foreground="#E91E63"/>

            <Border Grid.Row="1" CornerRadius="18" Background="#1E1E1E" BorderBrush="#444444" BorderThickness="1" Grid.ZIndex="1" Name="SearchBorder" Margin="0">
                <Grid Height="36">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Width="36" Height="36" Background="Transparent" BorderThickness="0" Margin="5,0,0,0">
                        <TextBlock Text="&#xE720;" FontSize="18" Foreground="#E91E63" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center"/>
                    </Button>

                    <TextBox x:Name="SearchTextBox" Grid.Column="1"
                             Text="Tra cứu từ, hoặc dịch văn bản nhanh..."
                             Style="{StaticResource SearchTextBoxStyle}"
                             Foreground="#F0F0F0" Background="{x:Null}" HorizontalAlignment="Left" VerticalAlignment="Center" BorderBrush="{x:Null}">
                    </TextBox>

                    <Button Grid.Column="2" x:Name="SearchButton" Width="36" Height="36" Background="Transparent" BorderThickness="0" Margin="0,0,5,0">
                        <TextBlock Text="&#xE721;" FontSize="16" Foreground="#E91E63" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center"/>
                    </Button>
                </Grid>
            </Border>

            <Border Grid.Row="2" Name="SearchHistoryPopup" 
                    Background="#1E1E1E" 
                    BorderBrush="#444444" BorderThickness="1"
                    CornerRadius="5" Margin="0,-5,0,0" Padding="0,0,0,5"
                    Visibility="Collapsed" Grid.ZIndex="1">
                <StackPanel>
                    <ListBox BorderThickness="0" Background="Transparent" MaxHeight="250" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ItemContainerStyle="{StaticResource ListBoxItemStyle}">

                        <ListBoxItem>
                            <Grid Margin="5,8">
                                <TextBlock Grid.Column="0" Text="&#xE100;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="#F0F0F0" Margin="0,0,10,0"/>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="question" FontWeight="Bold" FontSize="15" Foreground="#F0F0F0"/>
                                        <TextBlock Text=" ['kwɛstʃən] (n, v)" FontSize="12" Foreground="#AAAAAA" FontStyle="Italic" Margin="5,0,0,0"/>
                                    </StackPanel>
                                    <TextBlock Text="câu hỏi" FontSize="14" Foreground="#E91E63" Margin="0,2,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Style="{StaticResource DropdownActionButtonStyle}">
                                        <TextBlock Text="&#xE10A;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </ListBoxItem>
                    </ListBox>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="1" Margin="0,20,0,0" 
                 Padding="15" 
                 Background="#1E1E1E"
                 CornerRadius="8"
                 VerticalAlignment="Stretch">
            <StackPanel>

                <Grid Margin="0,0,0,10">
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="[Từ đang tra cứu]" FontSize="24" FontWeight="Bold" Foreground="#F0F0F0"/>
                        <TextBlock Text="/ˈdɪk.ʃə.nər.i/ (n)" FontSize="16" Foreground="#AAAAAA" FontStyle="Italic" Margin="0,4,0,0"/>
                    </StackPanel>

                    <Border Grid.Column="1" CornerRadius="18" Width="36" Height="36" Background="#442233">
                        <Button Background="Transparent" BorderThickness="0">
                            <TextBlock Text="&#xE767;" FontSize="20" Foreground="#E91E63" FontFamily="Segoe MDL2 Assets"/>
                        </Button>
                    </Border>
                </Grid>

                <Rectangle Height="1" Fill="#444444" Margin="0,5"/>

                <TextBlock Text="Định nghĩa:" FontSize="14" FontWeight="SemiBold" Foreground="#F0F0F0" Margin="0,10,0,5"/>
                <TextBlock Text="1. (Danh từ): Cuốn sách liệt kê các từ của một ngôn ngữ..."
                           TextWrapping="Wrap" FontSize="14" Foreground="#AAAAAA" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
