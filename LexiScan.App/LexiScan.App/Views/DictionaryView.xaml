<UserControl x:Class="LexiScan.App.Views.DictionaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="#F4F4F9">

    <UserControl.Resources>
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#E5E5E5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#D0D0D0"/>
                            </Trigger>
                            <Trigger Property="IsDefaulted" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#E0F7FA"/>
                                <Setter Property="Foreground" Value="#0C39E8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SidebarIconStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <Style TargetType="{x:Type TextBox}" x:Key="SearchTextBoxStyle">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="Foreground" Value="#000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DropdownActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ListBoxItemStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="Border" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E0F0FF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#D0E0FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Height="409" Width="649">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0" Padding="15">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,25">
                    <Border Width="40" Height="40" CornerRadius="20" Background="#0C39E8" VerticalAlignment="Center" Margin="5,0,10,0">
                        <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="White" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Người dùng" FontWeight="SemiBold" FontSize="14" Foreground="#333"/>
                        <TextBlock Text="Đã đăng nhập" FontSize="11" Foreground="#666"/>
                    </StackPanel>
                </StackPanel>

                <Button Style="{StaticResource SidebarButtonStyle}" IsDefault="True" Name="BtnHome">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE80F;" Style="{StaticResource SidebarIconStyle}"/>
                            <TextBlock Text="Trang chủ"/>
                        </StackPanel>
                    </Button.Content>
                </Button>

                <Button Style="{StaticResource SidebarButtonStyle}" Name="BtnDictionary">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE8B9;" Style="{StaticResource SidebarIconStyle}"/>
                            <TextBlock Text="Từ điển cá nhân"/>
                        </StackPanel>
                    </Button.Content>
                </Button>

                <Button Style="{StaticResource SidebarButtonStyle}" Name="BtnHistory">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE81C;" Style="{StaticResource SidebarIconStyle}"/>
                            <TextBlock Text="Lịch sử tra cứu"/>
                        </StackPanel>
                    </Button.Content>
                </Button>

                <Button Style="{StaticResource SidebarButtonStyle}" Name="BtnTranslation">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE756;" Style="{StaticResource SidebarIconStyle}"/>
                            <TextBlock Text="Dịch văn bản"/>
                        </StackPanel>
                    </Button.Content>
                </Button>

                <Rectangle Height="1" Fill="#E0E0E0" Margin="0,15"/>

                <Button Style="{StaticResource SidebarButtonStyle}" VerticalAlignment="Bottom" Margin="0,10,0,0" Name="BtnSettings">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE713;" Style="{StaticResource SidebarIconStyle}"/>
                            <TextBlock Text="Cài đặt"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="24,102,16,0" Height="85" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10*"/>
                <ColumnDefinition Width="97*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Grid.ColumnSpan="2" RenderTransformOrigin="0.497,1.105" Margin="0,12,0,-13" Grid.RowSpan="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="LexiScan App" FontSize="28" FontWeight="ExtraBold" Margin="0,0,0,10" Foreground="#0C39E8"/>

                <Border Grid.Row="1" CornerRadius="18" Background="White" BorderBrush="#CCCCCC" BorderThickness="1" Grid.ZIndex="1" Name="SearchBorder" Margin="0">
                    <Grid Height="36">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Width="36" Height="36" Background="Transparent" BorderThickness="0" Margin="5,0,0,0">
                            <TextBlock Text="&#xE720;" FontSize="18" Foreground="#0C39E8" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center"/>
                        </Button>

                        <TextBox x:Name="SearchTextBox" Grid.Column="1"
                                 Text="Tra cứu từ, hoặc dịch văn bản nhanh..."
                                 Style="{StaticResource SearchTextBoxStyle}"
                                 Foreground="#666666">
                            <TextBox.Triggers>
                                <EventTrigger RoutedEvent="GotFocus">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SearchHistoryPopup" Storyboard.TargetProperty="Visibility">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{x:Static Visibility.Visible}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="LostFocus">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SearchHistoryPopup" Storyboard.TargetProperty="Visibility">
                                                <DiscreteObjectKeyFrame KeyTime="0:0:0.1" Value="{x:Static Visibility.Collapsed}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </TextBox.Triggers>
                        </TextBox>

                        <Button Grid.Column="2" x:Name="SearchButton" Width="36" Height="36" Background="Transparent" BorderThickness="0" Margin="0,0,5,0">
                            <TextBlock Text="&#xE721;" FontSize="16" Foreground="#0C39E8" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center"/>
                        </Button>
                    </Grid>
                </Border>

                <Border Grid.Row="2" Name="SearchHistoryPopup" 
                        Background="White" 
                        BorderBrush="#CCCCCC" BorderThickness="1"
                        CornerRadius="5" Margin="0,-5,0,0" Padding="0,0,0,5"
                        Visibility="Collapsed" Grid.ZIndex="1">
                    <StackPanel>
                        <ListBox BorderThickness="0" Background="Transparent" MaxHeight="250" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ItemContainerStyle="{StaticResource ListBoxItemStyle}">

                            <ListBoxItem>
                                <Grid Margin="5,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="&#xE100;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="#666" Margin="0,0,10,0"/>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="question" FontWeight="Bold" FontSize="15" Foreground="#333"/>
                                            <TextBlock Text=" ['kwɛstʃən] (n, v)" FontSize="12" Foreground="#666" FontStyle="Italic" Margin="5,0,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="câu hỏi" FontSize="14" Foreground="#0C39E8" Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Button Style="{StaticResource DropdownActionButtonStyle}">
                                            <TextBlock Text="&#xE10A;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                                        </Button>
                                        <Button Style="{StaticResource DropdownActionButtonStyle}">
                                            <TextBlock Text="&#xE731;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </ListBoxItem>

                            <ListBoxItem>
                                <Grid Margin="5,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="&#xE100;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="#666" Margin="0,0,10,0"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="instrument" FontWeight="Bold" FontSize="15" Foreground="#333"/>
                                            <TextBlock Text=" ['ɪnstrəmənt] (n, v)" FontSize="12" Foreground="#666" FontStyle="Italic" Margin="5,0,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="dụng cụ, thiết bị phát ra âm thanh" FontSize="14" Foreground="#0C39E8" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Button Style="{StaticResource DropdownActionButtonStyle}">
                                            <TextBlock Text="&#xE10A;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                                        </Button>
                                        <Button Style="{StaticResource DropdownActionButtonStyle}">
                                            <TextBlock Text="&#xE731;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </ListBoxItem>

                            <ListBoxItem>
                                <Grid Margin="5,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="&#xE100;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="#666" Margin="0,0,10,0"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="ball" FontWeight="Bold" FontSize="15" Foreground="#333"/>
                                            <TextBlock Text=" [bɔːl] (n, v)" FontSize="12" Foreground="#666" FontStyle="Italic" Margin="5,0,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="quả bóng, quả banh" FontSize="14" Foreground="#0C39E8" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Button Style="{StaticResource DropdownActionButtonStyle}">
                                            <TextBlock Text="&#xE10A;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                                        </Button>
                                        <Button Style="{StaticResource DropdownActionButtonStyle}">
                                            <TextBlock Text="&#xE731;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </ListBoxItem>
                        </ListBox>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
    