<UserControl x:Class="LexiScan.App.Views.TranslationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LexiScan.App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" 
             Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="ActionIconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryAccent}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LanguageTabStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,3"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryAccent}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryAccent}"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </UserControl.Resources>

    <Border Background="{DynamicResource AppBackground}" CornerRadius="0,15,15,0" ClipToBounds="True">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Dịch văn bản" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryAccent}" Margin="0,0,0,10"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="8" Margin="0,5,10,120">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1">
                            <StackPanel Orientation="Horizontal" Margin="10,0" Height="45">
                                <Button Content="{Binding SourceLangName}" Style="{StaticResource LanguageTabStyle}"/>
                            </StackPanel>
                        </Border>

                        <ScrollViewer Grid.Row="1" Margin="10" VerticalScrollBarVisibility="Auto">
                            <TextBox x:Name="SourceTextBox" Text="{Binding SourceText, UpdateSourceTrigger=PropertyChanged}" 
                            MaxLength="1000" BorderThickness="0" Background="Transparent" 
                            TextWrapping="Wrap" FontSize="18" Foreground="{DynamicResource PrimaryText}" 
                            FontWeight="SemiBold" AcceptsReturn="True">

                                <TextBox.InputBindings>
                                    <KeyBinding Key="Return" Command="{Binding SaveTranslationCommand}" />
                                </TextBox.InputBindings>
                            </TextBox>
                        </ScrollViewer>

                        <Grid Grid.Row="2" Margin="10,0,10,10">
                            <StackPanel Orientation="Horizontal" Visibility="{Binding IsSourceEnglishVisible}">
                                <Button Style="{StaticResource ActionIconButton}" Command="{Binding StartVoiceCommand}">
                                    <TextBlock Text="&#xE720;" FontFamily="Segoe MDL2 Assets" FontSize="18"/>
                                </Button>
                                <Button Style="{StaticResource ActionIconButton}" Command="{Binding SpeakSourceCommand}" Margin="5,0,0,0">
                                    <TextBlock Text="&#xE767;" FontFamily="Segoe MDL2 Assets" FontSize="16"/>
                                </Button>
                               
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <TextBlock Text="{Binding CurrentCharCount}" Foreground="{DynamicResource SecondaryText}" FontSize="13"/>
                                <TextBlock Text="/ 1.000" Foreground="{DynamicResource BorderColor}" FontSize="13"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>

                <Border Grid.Column="1" VerticalAlignment="Center" Width="35" Height="35" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="17">
                    <Button Style="{StaticResource ActionIconButton}" Command="{Binding SwapLanguageCommand}">
                        <TextBlock Text="&#xE8CB;" FontFamily="Segoe MDL2 Assets" FontSize="16" RenderTransformOrigin="0.5,0.5">
                            <TextBlock.RenderTransform>
                                <RotateTransform Angle="-90"/>
                            </TextBlock.RenderTransform>
                        </TextBlock>
                    </Button>
                </Border>

                <Border Grid.Column="2" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="8" Margin="10,5,0,120">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1">
                            <StackPanel Orientation="Horizontal" Margin="10,0" Height="45">
                                <Button Content="{Binding TargetLangName}" Style="{StaticResource LanguageTabStyle}"/>
                            </StackPanel>
                        </Border>

                        <ScrollViewer Grid.Row="1" Margin="10" VerticalScrollBarVisibility="Auto">
                            <TextBlock Text="{Binding TranslatedText}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryText}" TextWrapping="Wrap"/>
                        </ScrollViewer>

                        <Grid Grid.Row="2" Margin="10,0,10,10">
                            <StackPanel Orientation="Horizontal" Visibility="{Binding IsTargetEnglishVisible}">
                                <Button Style="{StaticResource ActionIconButton}" Command="{Binding SpeakTargetCommand}">
                                    <TextBlock Text="&#xE767;" FontFamily="Segoe MDL2 Assets" FontSize="16"/>
                                </Button>
                                <Button Style="{StaticResource ActionIconButton}" Command="{Binding SaveTranslationCommand}" Margin="5,0,0,0">
                                    <TextBlock Text="&#xE734;" FontFamily="Segoe MDL2 Assets" FontSize="16"/>
                                </Button>
                            </StackPanel>
                            <TextBlock HorizontalAlignment="Right" VerticalAlignment="Bottom" Text="LexiScan Pro" FontSize="10" Foreground="{DynamicResource SecondaryText}" Opacity="0.5"/>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>