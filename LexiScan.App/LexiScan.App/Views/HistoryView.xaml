<UserControl x:Class="LexiScan.App.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="700"
             Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style TargetType="{x:Type TextBox}" x:Key="SearchHistoryTextBoxStyle">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
        </Style>

        <Style x:Key="DetailButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryAccent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DeleteIconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource BorderColor}"/>
                    <Setter Property="Foreground" Value="#FF4D4D"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border Background="{DynamicResource AppBackground}" 
            CornerRadius="0,15,15,0" 
            ClipToBounds="True">

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Lịch sử Tra cứu" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryAccent}"/>
                <TextBlock Text="Xem lại và quản lý các từ khóa bạn đã tra cứu." FontSize="14" Foreground="{DynamicResource SecondaryText}" FontStyle="Italic"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Margin="0,0,10,0" Padding="10" 
                        Background="{DynamicResource CardBackground}" CornerRadius="8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE721;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{DynamicResource PrimaryAccent}" VerticalAlignment="Center" Margin="0,0,10,0"/>

                        <TextBox Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource SearchHistoryTextBoxStyle}"
                                 Width="350"
                                 d:Text="Nhập từ khóa để tìm kiếm trong lịch sử..."/>
                    </StackPanel>
                </Border>

                <Button Grid.Column="1"
                        Content="Xóa toàn bộ Lịch sử"
                        Command="{Binding ClearHistoryCommand}"
                        Background="#FF4D4D" 
                        Foreground="White"
                        FontSize="12"
                        Padding="15,8"
                        HorizontalAlignment="Center"/>
            </Grid>

            <Border Grid.Row="2" Padding="10" 
                    Background="{DynamicResource CardBackground}" CornerRadius="8">

                <ListView ItemsSource="{Binding HistoryEntries}" 
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.ScrollUnit="Pixel"
                          BorderThickness="0"
                          Background="Transparent">

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0,5"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,5">

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding SearchTerm}" 
                                                   FontSize="16" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="{DynamicResource PrimaryAccent}"
                                                   TextTrimming="CharacterEllipsis"
                                                   MaxHeight="60"
                                                   TextWrapping="Wrap"
                                                   ToolTip="{Binding SearchTerm}"/>

                                        <TextBlock Text="{Binding DisplayTime}" 
                                                   FontSize="12" 
                                                   Foreground="{DynamicResource SecondaryText}"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                                        <Button Content="Xem Chi Tiết" 
                                                Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListView}}, Path=DataContext.ViewDetailsCommand}"
                                                CommandParameter="{Binding .}"
                                                Margin="0,0,10,0"
                                                Style="{StaticResource DetailButtonStyle}"/>

                                        <Button Content="&#xE74D;" 
                                                FontFamily="Segoe MDL2 Assets"
                                                Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListView}}, Path=DataContext.DeleteHistoryEntryCommand}"
                                                CommandParameter="{Binding .}"
                                                Style="{StaticResource DeleteIconButtonStyle}"/>
                                    </StackPanel>
                                </Grid>

                                <Border Background="{DynamicResource AppBackground}" 
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1"
                                        CornerRadius="5"
                                        Margin="0,8,0,0"
                                        Padding="15"
                                        Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVis}}">

                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" Margin="0,0,0,10">
                                            <materialDesign:PackIcon Kind="ProgressDownload" Width="20" Height="20" Margin="0,0,10,0" Foreground="{DynamicResource PrimaryAccent}">
                                                <materialDesign:PackIcon.RenderTransform>
                                                    <RotateTransform Angle="0"/>
                                                </materialDesign:PackIcon.RenderTransform>
                                            </materialDesign:PackIcon>
                                            <TextBlock Text="Đang tải dữ liệu..." VerticalAlignment="Center" Foreground="{DynamicResource SecondaryText}" FontStyle="Italic"/>
                                        </StackPanel>

                                        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="350">
                                            <StackPanel>

                                                <TextBlock Text="VĂN BẢN GỐC:" 
                                                           FontSize="11" 
                                                           FontWeight="Bold" 
                                                           Foreground="{DynamicResource SecondaryText}" 
                                                           Opacity="0.7"
                                                           Margin="0,0,0,5"/>

                                                <TextBlock Text="{Binding SearchTerm}" 
                                                           TextWrapping="Wrap" 
                                                           FontSize="15" 
                                                           FontWeight="Medium"
                                                           Foreground="{DynamicResource PrimaryAccent}"
                                                           Margin="0,0,0,15"/>

                                                <Separator Background="{DynamicResource BorderColor}" Margin="0,0,0,15"/>

                                                <TextBlock Text="NGHĨA CHI TIẾT:" 
                                                           FontSize="11" 
                                                           FontWeight="Bold" 
                                                           Foreground="{DynamicResource SecondaryText}" 
                                                           Opacity="0.7"
                                                           Margin="0,0,0,5"/>

                                                <TextBlock Text="{Binding DetailInfo}" 
                                                           TextWrapping="Wrap" 
                                                           FontSize="14" 
                                                           LineHeight="24"
                                                           Foreground="{DynamicResource PrimaryText}">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                            </StackPanel>
                                        </ScrollViewer>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </Grid>
    </Border>
</UserControl>