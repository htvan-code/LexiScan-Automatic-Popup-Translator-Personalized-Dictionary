<UserControl x:Class="LexiScan.App.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="700"
             Background="#121212">
    <!-- BackgroundColor: #121212 -->
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBox}" x:Key="SearchHistoryTextBoxStyle">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#F0F0F0"/>
            <!-- PrimaryForeground: #F0F0F0 -->
        </Style>

        <Style x:Key="DetailButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E91E63"/>
            <!-- PrimaryColor: #E91E63 -->
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DeleteIconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <!-- SecondaryForeground: #AAAAAA -->
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#442233"/>
                    <!-- SelectedBackgroundColor: #442233 -->
                    <Setter Property="Foreground" Value="#FF4D4D"/>
                    <!-- DangerColor: #FF4D4D -->
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Tiêu đề và Nút hành động -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Lịch sử Tra cứu" FontSize="28" FontWeight="Bold" Foreground="#FFE91E63"/>
            <!-- PrimaryForeground: #F0F0F0 -->
            <TextBlock Text="Xem lại và quản lý các từ khóa bạn đã tra cứu." FontSize="14" Foreground="#AAAAAA" FontStyle="Italic"/>
            <!-- SecondaryForeground: #AAAAAA -->
        </StackPanel>

        <!-- Row 1: Thanh Tìm kiếm và Nút Xóa -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Thanh Tìm kiếm/Lọc - SidebarColor: #1E1E1E -->
            <Border Grid.Column="0" Margin="0,0,10,0" Padding="10" 
                    Background="#1E1E1E" CornerRadius="8">
                <StackPanel Orientation="Horizontal">
                    <!-- PrimaryColor: #E91E63 -->
                    <TextBlock Text="&#xE721;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#E91E63" VerticalAlignment="Center" Margin="0,0,10,0"/>

                    <TextBox Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                                Style="{StaticResource SearchHistoryTextBoxStyle}"
                                Foreground="#F0F0F0"
                                Width="350"
                                d:Text="Nhập từ khóa để tìm kiếm trong lịch sử..."/>
                </StackPanel>
            </Border>

            <!-- Nút Xóa Lịch sử - DangerColor: #FF4D4D -->
            <Button Grid.Column="1"
                       Content="Xóa Toàn bộ Lịch sử"
                       Command="{Binding ClearHistoryCommand}"
                       Background="#FF4D4D" 
                       Foreground="White"
                       FontSize="14"
                       Padding="15,8"
                       HorizontalAlignment="Right"/>
        </Grid>

        <!-- Row 2: Hiển thị Danh sách Lịch sử - SidebarColor: #1E1E1E -->
        <Border Grid.Row="2" Padding="10" 
                Background="#1E1E1E" CornerRadius="8">
            <ListView ItemsSource="{Binding HistoryEntries}" 
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      BorderThickness="0"
                      Background="Transparent">

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0,5"/>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding SearchTerm}" FontSize="16" FontWeight="SemiBold" Foreground="#E91E63"/>
                                <!-- PrimaryColor: #E91E63 -->
                                <TextBlock Text="{Binding DisplayTime}" FontSize="12" Foreground="#AAAAAA"/>
                                <!-- SecondaryForeground: #AAAAAA -->
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="Xem Chi Tiết" 
                                           Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListView}}, Path=DataContext.ViewDetailsCommand}"
                                           CommandParameter="{Binding .}"
                                           Margin="0,0,10,0"
                                           Style="{StaticResource DetailButtonStyle}"/>

                                <Button Content="&#xE74D;" 
                                           FontFamily="Segoe MDL2 Assets"
                                           Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListView}}, Path=DataContext.DeleteItemCommand}"
                                           CommandParameter="{Binding .}"
                                           Style="{StaticResource DeleteIconButtonStyle}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

    </Grid>
</UserControl>
