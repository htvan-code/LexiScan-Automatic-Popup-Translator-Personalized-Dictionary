<UserControl x:Class="LexiScan.App.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:LexiScan.App.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="700"
             Background="{DynamicResource BackgroundColor}"
             >
    <UserControl.Resources>
        <!-- STYLE MỚI CHO TEXTBOX TÌM KIẾM -->
        <Style TargetType="{x:Type TextBox}" x:Key="SearchHistoryTextBoxStyle">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
            <!-- Thêm Template để đảm bảo nền trong suốt -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style cho nút Xem Chi Tiết -->
        <Style x:Key="DetailButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryHoverColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style cho nút Icon Xóa -->
        <Style x:Key="DeleteIconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryForeground}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource BorderColor}"/>
                    <Setter Property="Foreground" Value="#FF4D4D"/>
                    <!-- Giữ màu đỏ cho Delete (Chỉ Foreground) -->
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề và Nút hành động -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Lịch sử Tra cứu" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryColor}"/>
            <TextBlock Text="Xem lại và quản lý các từ khóa bạn đã tra cứu." FontSize="14" Foreground="{DynamicResource SecondaryForeground}" FontStyle="Italic"/>
        </StackPanel>

        <!-- Thanh Tìm kiếm và Nút Xóa -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Thanh Tìm kiếm/Lọc -->
            <!-- Sử dụng SidebarColor làm nền cho Border -->
            <Border Grid.Column="0" Background="{DynamicResource SidebarColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <!-- Icon Tìm kiếm -->
                    <TextBlock Text="&#xE721;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{DynamicResource PrimaryColor}" VerticalAlignment="Center" Margin="0,0,10,0"/>

                    <!-- TextBox đã được áp dụng Style mới -->
                    <TextBox Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource SearchHistoryTextBoxStyle}" 
                             Background="Transparent" 
                             Width="350"
                             d:Text="Nhập từ khóa để tìm kiếm trong lịch sử..."
                             />
                </StackPanel>
            </Border>

            <!-- Nút Xóa Lịch sử -->
            <Button Grid.Column="1"
                    Content="Xóa Toàn bộ Lịch sử"
                    Command="{Binding ClearHistoryCommand}"
                    Background="{DynamicResource PrimaryColor}" 
                    Foreground="White"
                    FontSize="14"
                    Padding="15,8"
                    HorizontalAlignment="Right">
                <!-- Thao tác Delete (Xóa) sử dụng PrimaryColor và PrimaryHoverColor -->
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <!-- Sử dụng PrimaryHoverColor để có hiệu ứng rê chuột đúng theme -->
                                <Setter Property="Background" Value="{DynamicResource PrimaryHoverColor}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <!-- Hiển thị Danh sách Lịch sử -->
        <Border Grid.Row="2" Background="{DynamicResource SidebarColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="8" Padding="10">
            <ListView ItemsSource="{Binding FilteredHistoryItems}" 
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      BorderThickness="0"
                      Background="Transparent">

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Margin" Value="0,2"/>
                        <Setter Property="Padding" Value="0,5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Grid>

                                        <Border x:Name="Border" Background="Transparent" Padding="10">
                                            <ContentPresenter/>
                                        </Border>
                                        <!-- Đường phân cách -->
                                        <Border Height="1" Background="{DynamicResource BorderColor}" VerticalAlignment="Bottom" Margin="0,5,0,0"/>

                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <!-- Sử dụng Setter trực tiếp với DynamicResource cho MouseOver -->
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource SelectedBackgroundColor}"/>
                                        </Trigger>
                                        <!-- Bổ sung Trigger cho IsSelected để thay đổi màu nền nếu cần -->
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource SelectedBackgroundColor}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <!-- Từ khóa tra cứu -->
                                <TextBlock Text="{Binding Keyword}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryColor}"/>
                                <!-- Thời gian tra cứu -->
                                <TextBlock Text="{Binding SearchDate, StringFormat='dd/MM/yyyy HH:mm:ss'}" FontSize="12" Foreground="{DynamicResource SecondaryForeground}"/>
                            </StackPanel>

                            <!-- Nút Xem chi tiết hoặc Xóa từng mục -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="Xem Chi Tiết" 
                                        Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListView}}, Path=DataContext.ViewDetailsCommand}"
                                        CommandParameter="{Binding .}"
                                        Margin="0,0,10,0"
                                        Style="{StaticResource DetailButtonStyle}"/>

                                <Button Content="&#xE74D;" 
                                        FontFamily="Segoe MDL2 Assets"
                                        Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListView}}, Path=DataContext.DeleteItemCommand}"
                                        CommandParameter="{Binding .}"
                                        Style="{StaticResource DeleteIconButtonStyle}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

    </Grid>
</UserControl>