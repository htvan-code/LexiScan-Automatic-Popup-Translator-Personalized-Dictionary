<UserControl x:Class="LexiScan.App.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:LexiScan.App.ViewModels" d:DataContext="{d:DesignInstance Type=vm:HistoryViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#F4F4F9">



    <UserControl.Resources>
        <Style x:Key="ActionIconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🕒 Lịch Sử Tra Cứu" 
                   FontSize="28" FontWeight="Bold" 
                   Margin="0,0,0,20" Foreground="#0C39E8"/>

        <ListView Grid.Row="1" ItemsSource="{Binding HistoryEntries}" 
                  Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Bên trái -->
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="5,0,0,0">
                            <TextBlock Text="{Binding SearchTerm}" 
                           FontWeight="SemiBold" FontSize="16" Foreground="#333"/>
                            <TextBlock Text="{Binding DisplayTime}" 
                           FontSize="12" Foreground="#999" Margin="0,2,0,0"/>
                        </StackPanel>

                        <!-- Icon sát lề phải, không spacing -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal" 
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right"
                        Margin="0,0,0,0">

                            <!-- Mic -->
                            <Button Style="{StaticResource ActionIconButton}" 
                        Margin="0" Padding="0"
                        Width="28" Height="28">
                                <TextBlock Text="&#xE767;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                            </Button>

                            <!-- Thùng rác -->
                            <Button Style="{StaticResource ActionIconButton}" 
                        Margin="0" Padding="0"
                        Width="28" Height="28"
                        Command="{Binding DataContext.DeleteHistoryEntryCommand, 
                                          RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListView}}}"
                        CommandParameter="{Binding}">
                                <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                            </Button>

                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>

        <Border Grid.Row="2" CornerRadius="5" Background="#FFEEEE" 
                Margin="0,20,0,0" HorizontalAlignment="Center">
            <Button Content="🗑️ Xóa Toàn Bộ Lịch Sử" 
                    Command="{Binding ClearHistoryCommand}"
                    Background="Transparent" 
                    Foreground="#D50000" 
                    FontWeight="SemiBold" 
                    BorderThickness="0"
                    Padding="20,10"/>
        </Border>
    </Grid>
</UserControl>